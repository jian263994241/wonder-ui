import { generateUtilityClasses } from '@wonder-ui/utils';

export const COMPONENT_NAME = 'WuiCascaderView';

export const cascaderViewClasses = generateUtilityClasses(COMPONENT_NAME, [
  'root',
  'tabs',
  'tabpanel',
  'content',
  'item'
]);
