import { Space, Button } from '@wonder-ui/core';

export default () => (
  <Space itemEqual>
    <Button variant="contained" fullWidth>
      Button
    </Button>
    <Button variant="contained" fullWidth>
      Button
    </Button>
    <Button variant="contained" fullWidth>
      Button
    </Button>
  </Space>
);
