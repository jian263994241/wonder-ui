import { Button, Space } from '@wonder-ui/core';

export default () => (
  <Space direction="vertical">
    <Button variant="contained" fullWidth>
      Block button
    </Button>
    <Button variant="contained" fullWidth>
      Block button
    </Button>
  </Space>
);
