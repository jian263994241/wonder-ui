import { Tabs, Tab } from '@wonder-ui/core';

export default () => (
  <Tabs showIndicator>
    <Tab
      wrapped
      label="New Arrivals in the Longest Text of Nonfiction that should appear in the next line"
    />
    <Tab label="New Arrivals in the Longest Text of Nonfiction that should appear in the next line" />
  </Tabs>
);
